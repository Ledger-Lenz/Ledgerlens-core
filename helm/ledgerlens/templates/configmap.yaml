apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "ledgerlens.fullname" . }}-config
  labels:
    {{- include "ledgerlens.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
